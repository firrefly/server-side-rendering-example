import { Consts } from "@utils";

const { PREFIX } = Consts.ACTIONS;

export const REVIEWS_SAVE = `${PREFIX}reviews/save`;

export const REVIEWS_PENDING = `${PREFIX}reviews/pending`;
export const REVIEWS_SUCCESS = `${PREFIX}reviews/success`;
export const REVIEWS_ERROR = `${PREFIX}reviews/error`;
