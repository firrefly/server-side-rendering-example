// Modules
import css from "styled-jsx/css";

export const styles = css`
  .hits__list {
    display: flex;
    flex-wrap: wrap;
    margin: -10px 0 0 -10px;
  }

  @media (max-width: 768px) {
    .header-hits {
      text-align: center;
    }
  }
`;
