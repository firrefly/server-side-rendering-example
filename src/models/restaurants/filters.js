// Utils
import { Consts } from "@utils";

const {
  OPTIONS_FILTER,
  FOOD_FILTERS,
} = Consts.FILTERS;

export const restaurantsFiltersModel = {
  options: OPTIONS_FILTER,
  foodList: FOOD_FILTERS,
};
