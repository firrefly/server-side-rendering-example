import { Consts } from "@utils";

const { PREFIX } = Consts.ACTIONS;

export const HITS_SAVE = `${PREFIX}hits/save`;

export const HITS_PENDING = `${PREFIX}hits/pending`;
export const HITS_SUCCESS = `${PREFIX}hits/success`;
export const HITS_ERROR = `${PREFIX}hits/error`;
